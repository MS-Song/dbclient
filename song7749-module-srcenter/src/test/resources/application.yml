##############################################################################
# 공통 실행 환경 
##############################################################################
# server define
# spring config
spring:
  # profile active
  profiles:
    active:
    - test

  # banner
  banner:
    charset: utf-8
    location: classpath:config/srcenter-banner.txt
  # cache 
  cache:
    ehcache:
      config: classpath:ehcache/ehcache.xml
  
  # datasource    
  datasource:
    url:  jdbc:h2:file:~/dbclient_test
    username: sa
    password: 
    driver-class-name: org.h2.Driver
    databaseName: DB Client Local Database Test
    schemaName: PUBLIC
    host: jdbc:h2:file:~/dbclient_test
    port: 
 # 스프링 영속성을 Controller 까지 확대. http://kingbbode.tistory.com/27 참고 바람
  jpa: 
    open-in-view: true